<%= erb(:nav, :layout => false) %>

<section class="content hidden" id="business-overview">
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-2">
        <div id="add">
          <button class="add-button" type="button" data-toggle="modal" data-target="#form"><img src="./assets/plus_icon.png"></button>
          <h6>New Program</h6>
          <!-- The add button pops out the following modal -->
            <%= erb(:modal, :layout => false) %>
          <!--  -->
        </div>
      </div>
      <%= erb(:statsbox, :layout => false) %>
    </div>

    <div class="row">
      <div class="col-md-9 col-md-offset-2">
        <%= erb(:monthly, :layout => false) %>
      </div>
    </div>
  </div>
</section>


